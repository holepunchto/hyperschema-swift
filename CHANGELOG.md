# Changelog

## 1.0.0

First stable release.

- Generates a Swift package (`Sources/Schema.swift`, `Package.swift`, `schema.json`) from a hyperschema definition.
- Generated `Package.swift` pins `compact-encoding-swift` with `from: "1.0.0"`.
