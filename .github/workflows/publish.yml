name: Publish
on:
  push:
    tags:
      - v*
permissions:
  id-token: write
  contents: read
jobs:
  publish:
    runs-on: ubuntu-latest
    name: Publish
    steps:
      - uses: holepunchto/actions/node-base@v1
      - run: npm publish --ignore-scripts
